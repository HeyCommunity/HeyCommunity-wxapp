<view class="container-full" id="page-posts-detail">
  <van-notify id="van-notify"></van-notify>

  <view wx:if="{{ models.length > 0 }}" class="items-post">
    <post-card posts="{{ models }}" is-detail="{{ true }}"></post-card>
  </view>
</view>
